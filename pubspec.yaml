name: webview_demo
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0-242.0.dev <4.0.0"
  flutter: ">=3.27.0-1.0.pre.50"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  jni:
    git:
      url: https://github.com/dart-lang/native/
      ref: main
      path: pkgs/jni

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  jnigen:
    git:
      url: https://github.com/dart-lang/native/
      ref: main
      path: pkgs/jnigen

dependency_overrides: 
  meta: 1.12.0

flutter:
  uses-material-design: true
